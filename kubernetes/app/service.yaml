apiVersion: v1
kind: Service
metadata:
  labels:
    app: blog
  name: blog
  namespace: app
spec:
  ports:
  - port: 8080
    protocol: TCP
    name: blog
    targetPort: 8080
  selector:
    app: blog
  type: NodePort
